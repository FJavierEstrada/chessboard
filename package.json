{
  "name": "Chessboard",
  "private": true,
  "version": "0.0.1",
  "description": "Chessboard sample",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@stencil/core": "^1.12.5",
    "@types/puppeteer": "2.0.1",
    "jest": "24.9.0",
    "jest-cli": "24.9.0",
    "puppeteer": "2.1.1"
  },
  "license": "MIT",
  "dependencies": {
    "@types/jest": "^25.2.1",
    "redux": "^4.0.5"
  }
}
